11월 6일 
개발일지 
오늘 알게된 것으로는 시큐리티레벨에서 목mvc테스트간 너무 광범위하게 빈을 등록하기에 오히려 초기 익셉션 핸들러 설계는 내부에 시큐리티나 별도 내부 컨트롤러를 설계하고 진행하며 그렇게 해야 나중에 깨지지않는다.
그리고 테스트는 배포 전 너무나도 중요하다. 기능졀로 어디에 문제가 생기는 지 알 수 있다.
이젠트 리스너의 생명주기는 정말 중요하다
jpa의 컬렉션 테이블과 컬랙션 엘리멘트는 1계층까지만 허용이다
그렇기에 jpa에서 깊이있는 연관관계 설계를 위해서는 값 객체가 아닌 결국 id값을 가지는 연관관계 엔티티관계를 맺도록 설계하고 내부 어그리게이션 루트에서 설정하되 각각의 연관관계 매핑 메서드를 만들고 그로 매핑 관계를 맺도록 설계를 해야된다. 그렇지 않으면 오히려 리플랙션을 통한 안좋은 구조가 된다.